module.exports = function lastIndexOf (str="", search,fromIndexopt = str.length-1)
    {
        let index = str.lastIndexOf(search,fromIndexopt); // retourne l'index ou se trouve le dernier caractere renseigné
        return index;
    
    }
         
         